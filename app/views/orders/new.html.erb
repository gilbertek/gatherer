<%= form_for @order do  |f| %>
  <% @order.errors.full_messages.each do |error| %>
    <p><%= error %></p>
  <% end %>

  <%= f.label :phone_number %>
  <%= f.text_field :phone_number %>

  <%= f.submit id: 'submit' %>
<% end %>
