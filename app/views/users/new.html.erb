<%= form_for @user do  |f| %>
  <% @user.errors.full_messages.each do |error| %>
    <p><%= error %></p>
  <% end %>

  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.submit id: 'submit' %>
<% end %>
